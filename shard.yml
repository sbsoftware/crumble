name: crumble
version: 0.11.5

dependencies:
  pg:
    github: will/crystal-pg
  to_html:
    github: sbsoftware/to_html.cr

targets:
  crumble:
    main: src/cli.cr
scripts:
  postinstall: shards build
executables:
  - crumble
